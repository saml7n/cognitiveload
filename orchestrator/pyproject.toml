[project]
name = "orchestrator"
version = "0.1.0"
description = "Orchestrator for Devin Triage Bot"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.20.0",
    "pydantic>=2.9.0",
    "requests>=2.30.0",
    "PyGithub>=2.1.0",
    "jinja2>=3.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "respx>=0.21.0",
]
